# DS4 decode: глубокий ресёрч и гипотезы подъёма (2026-07-10)

Цель: decode ≥ 35 t/s на всей длине контекста (сейчас 36.3 мелко / 31.6 @97K
без MTP), плюс разобраться, лечится ли EP-decode (36.34 → 23.82 t/s при
expert parallel). Пока ТОЛЬКО ресёрч — ничего не имплементировано.

Источники: три параллельных ресёрч-потока (веб: комм-латентность; веб:
спекулятивка/MoE; nsys-профилировка EP-пути на риге через фикстуру) + доки
Codex (`DS4HANDOFF.md`, `DS4_DECODE_RESEARCH_2026-07-10.md`,
`DS4_EXPERT_PARALLEL_2026-07-10.md`, `DS4_RESEARCH_2026-07-09.md` на риге в
`/mnt/ssd/engines/llama.cpp-v4-cchuter/`) + память `ds4-longctx-push`.

---

## Часть 1. Анатомия EP-штрафа: 14.5 мс/токен разложены по полочкам

Профилировка nsys на real-weight 4-слойной фикстуре ВОСПРОИЗВЕЛА штраф
полной модели: EP decode 4.72 мс/ток vs layer 3.34 мс/ток = **+345 мкс на
MoE-слой** (полная модель: 14.46 мс / ~42 слоя = 344 мкс — совпадение 1:1).
Репорты на риге: `/tmp/ep_prof.nsys-rep`, `/tmp/layer_prof.nsys-rep`,
`/tmp/perfaudit-*.log`.

Верифицированные факты модели: n_embd=4096, n_expert=256, **n_expert_used=6**,
n_ff_exp=2048, 1 shared expert. AR-payload на слой при decode = 4096×F32 =
**16.4 KB** (BF16 включается только с ne≥262144, ggml-cuda.cu:1209).

### Куда уходят 345 мкс/слой (замерено, не гипотеза)

| # | Компонент | Стоимость | Причина |
|---|---|---:|---|
| 1 | Host-sync dead air между сплитами | ~100–150 мкс | meta-бэкенд объявляет `caps.events=false` (ggml-backend-meta.cpp:131-136) → sched делает host-blocking `ggml_backend_synchronize` на каждый вход сплита (ggml-backend.cpp:1578-1584) и выключает pipeline parallelism (llama-context.cpp:323-350). ~25 лишних `cudaStreamSynchronize`/слой (9259 vs 2897 на прогон) |
| 2 | Серийный запуск 4 подграфов | ~90 мкс | host по очереди зовёт 4× `cudaGraphLaunch` (~20-24 мкс API каждый) → лесенка стартов ~30 мкс между устройствами |
| 3 | NCCL AllReduce | истинная цена 28–36 мкс, медиана 118 мкс | кернел раннего прибывшего СПИНИТ, ожидая отстающих из-за лесенки #2. Один AR/слой (delayed-AR паттерн работает), F32, RING_LL, запускается ВНЕ CUDA-графов |
| 4 | Дисбаланс шардов + фикс-оверхед MMQ | ~50–80 мкс | top-6 из 256 при 64/GPU: часть GPU владеет 0 экспертами токена, но всё равно гоняет полный пайплайн (memset+компакция+quantize+пустой MMQ ≈ 55 мкс); самый занятой GPU (2-3 эксперта) идёт через **MMQ tile path вместо MMVQ** (форс в ggml-cuda.cu:2642-2652) и гейтит AR |
| 5 | Dispatch-копии (3 входа × 4 зеркала) | ~7 мкс GPU | сами копии дёшевы; их API/event-хореография уже учтена в #1 |

**Ключевые развенчания:**
- **CUDA-графы в EP-режиме РАБОТАЮТ** (34 захвата → 1770 replay; в steady
  decode вне графов только NCCL). Это не источник штрафа. Но: rebuild-кэш
  meta-бэкенда — один скаляр `uid` (:1582), а sched даёт каждому из ~43
  meta-сплитов свой uid → `needs_rebuild` срабатывает на КАЖДЫЙ вызов,
  host заново гоняет split-state анализ + полный property-memcmp каждого
  CUDA-графа вместо быстрого uid-шортката.
- **Это не NCCL и не PCIe.** Замеренный пол NCCL AR 16 KB на 4×GPU PCIe
  P2P (4×4090): ~10.7 мкс; модель NVIDIA для Ring LL на 4 GPU: ~12.6 мкс.
  Провода для 16 KB: <1 мкс. Наши 340 мкс/слой — в 10-15 раз выше пола,
  и это почти целиком host-оркестрация.
- Codex-вывод «FP32 AR и manual reduce-to-one медленнее» этим объясняется:
  мы launch/sync-bound, а не wire-bound — смена формата на проводе не
  может помочь. Кстати, «internal 2-GPU AllReduce» (`allreduce.cu`) ходит
  через **pinned host memory** — он писался до P2P-драйвера и рабочий P2P
  не использует вообще.

### Контекст: P2P на риге ЖИВ

Кастомный драйвер 610.43.02 стоит, `nvidia-smi topo -p2p r` = OK на всех
парах. Гипотеза P1-C из DS4_RESEARCH_2026-07-09 писалась ещё при «NCCL
uses shared host memory because CUDA P2P is unavailable» — экономика
изменилась, кастомный one-shot P2P-путь стал реалистичным. (Память
`rig-p2p-patch-status` обновлена; риск: kernel-апдейт + DKMS может тихо
вернуть сток — проверять после каждого апдейта ядра.)

---

## Часть 2. Ранжированные гипотезы

### Трек A: decode ≥35 на всей длине (кратчайший путь — БЕЗ EP)

**A1. Замерить прод MTP @97K с fused decode indexer. 0 кода.**
31.6 t/s (no-MTP, fused idx) × измеренный MTP-множитель ×1.25–1.5 =
**39–47 t/s @97K** — цель 35 вероятно УЖЕ взята текущим прод-конфигом,
просто никто не мерил MTP на 97K после fused indexer (последний замер MTP
на длине: 31.5 @21K, до fused idx). Спекулятивка на длинном контексте
относительно ВЫГОДНЕЕ: доминирующая стоимость шага (KV + индексер-скан)
шарится на verify-батч. Это эксперимент №1 по соотношению цена/информация.

**A2. Ревайв MTP K=2 — запаркованный ×0.91 почти наверняка баг, а не
свойство модели.** Публичные данные по НАШЕЙ модели: vLLM/H200 DSv4-Flash
MTP K=1 = ×1.49 @bs1, потолок K≥2 оценён ~×1.85–2.0 (×2.03 опубликовано
на B300), MTP-блок V4-Flash «designed for higher k»
(github.com/vllm-project/vllm/issues/43457). Conditional acceptance глубины
2 в деплоях: 0.35–0.65 (SGLang NextN: draft=2 → accept len ~1.9; LMSYS
long-ctx: 2.18 при draft=3). У нас приёмка level-2 была ~0 — с вероятностью
~90% баг ветки (несвежие логиты/индексация уровня-2, как уже было с
argmax-веткой). Ожидание: **+8–25% поверх K=1** (центр ~+15%), adaptive-K
(драфтить 2-й только при высокой уверенности, SpecDec++/DSpark-style) двигает
к +15–30%. Известные грабли из парковки: nt=3 verify 68 мс — replay CUDA-графа
не выживал на тройке; чинить надо и это (const-shape бакет под nt=3), и
приёмку (офлайн D2-тест как фаза A, ДО серверной интеграции).

**A3. Переиспользование индексера на длинном контексте — главный резерв
@97K+.** На 131K индексер-скан = >90% чтений attention-части шага (индексер
читает ВЕСЬ компресс-кэш, sparse-attention потом читает фикс. top-512).
Наши 36.3→31.6 (мелко→97K) — это ровно он. Приёмы из литературы:
- **Cross-layer reuse (IndexCache, arxiv 2603.12201):** индексер гоняется
  на каждом 3-4-м слое, остальные переиспользуют ближайший top-k. Убирает
  75% индексер-компьюта, **×1.48 decode на длинном ctx**, качество ~не
  страдает. У нас 22 CSA-слоя → индексер на 6-8 из них.
- **Temporal reuse (LSA, arxiv 2606.09079):** пере-скан раз в τ шагов
  (у них τ=64!), между рескaнaми — отбор из сохранённого candidate-superset.
  Консервативный вариант: superset top-2048 раз в 4-8 шагов, точный
  re-rank внутри него каждый шаг.
- Квантование индекс-кэша: FP8 — норма в vLLM/SGLang DSA; на Ампере аналог
  int8 → минус половина байтов скана. Отдельный, складывающийся рычаг.
Риск: качество отбора → гейт needle@200K + KL на логитах. Ожидание:
**+15–48% decode на 97K+**, на мелком ~0.

**A4. Порт свежих upstream MoE-decode оптимизаций llama.cpp.** TopK-MoE
fusion (#16130), GEMV fusion mul_mat_vec+ADD/GLU (#16715), mmvq bias/gate
load-latency (#16847), MMVQ мелкобатч +6-7% @batch 8 (#25445 — ровно
verify-режим). Единицы процентов каждый, но складываются и почти без риска.

**A5. Lock клоков (не power).** 220→300W в почти нашем сетапе (6×3090,
2-бит MoE) даёт лишь +4-5% decode. Но в layer-split пайплайне каждый GPU
активен ~25% шага и DVFS роняет клоки между окнами — `nvidia-smi -lgc`
(~1400-1700 МГц) при тех же 220-250W целит именно в это. Ожидание 0-10%,
цена нулевая, мерить первым же стендом.

**A6. Реквант down Q2_K→IQ2_XXS ради decode — ОТМЕНЯЕТСЯ.** Преимущество
IQ2_XXS ×1.74 — это MMQ (prefill). В MMVQ/GEMV i-кванты не быстрее, бывают
медленнее Q2_K (замер: IQ2_XXS на 9% медленнее в TG). Как prefill-рычаг
(+5-7%) и VRAM-рычаг остаётся, но из decode-трека выходит.

### Трек B: лечение EP-decode (если хотим один конфиг на всё)

Порядок = ранжирование по (экономия / усилие). Оценки — по nsys-разбивке.

**B1. Events на meta-бэкенде.** Пробросить event_new/record/wait к четырём
CUDA-бэкендам (record-on-all/wait-on-all) → sched перестаёт делать
host-blocking sync на каждый вход сплита, host убегает вперёд. ~100-150
мкс/слой. Грабля: с n_copies>1 поедут указатели input-копий и сломается
стабильность CUDA-граф-ключей → либо оставить n_copies=1 при живых events
(маленький патч sched), либо принять ×4 граф-вариантов.

**B2. Per-split rebuild-кэш в meta-бэкенде.** Заменить скалярный uid на
map по uid сплита (ggml-backend-meta.cpp:1775/1967). Тривиальный дифф,
убирает пер-слойный host resplit + возвращает uid-шорткат CUDA-графам.
~30-80 мкс/слой host critical path (умножается с B1).

**B3. Параллельный запуск 4 подграфов + NCCL из 4 потоков** (или
persistent per-device worker) в ggml-backend-meta.cpp:2173-2192. Убирает
лесенку ~90 мкс, которую AR сейчас съедает спином. ~60-90 мкс/слой.

**B4. Capturable P2P reduce-кернел вместо NCCL на decode (эндгейм).**
Remote-устройства P2P-пушат свой 16 KB partial + флаг прямо в буфер
home-устройства; home складывает 3 вектора. Обычный кернел → капчится в
CUDA-граф, слой реплеится целиком без участия host. Причём достаточно
**reduce-to-home** (следующий потребитель — attention home-слоя; следующий
слой всё равно ре-бродкастит) — вдвое меньше трафика, чем AllReduce.
Прецеденты: vLLM custom_all_reduce (one-shot, IPC-буферы, капчится в
графы; на PCIe работает при форсе P2P), TRT-LLM MIN_LATENCY (Lamport
flags), JAX/XLA single-shot AR+RMSNorm fusion (~3× кернел-тайм, +27% e2e
decode). AR-фаза 180-250 → 30-50 мкс/слой. ВАЖНО: NCCL в CUDA-графах в
однопроцессном multi-GPU — документированный дедлок-кейс, кастомный кернел
этой проблемы не имеет (ещё один довод против NCCL на decode-пути).

**B5. Срезать zero-work на GPU без экспертов + вернуть MMVQ.** Свернуть
memset'ы в эпилог компакции, device-side guard на `expert_bounds[n]==0`
мимо quantize, и разобраться с форсом MMQ вместо MMVQ на decode в EP-пути
(ggml-cuda.cu:2642-2652) — layer-split на decode идёт векторным MMVQ, EP
гоняет тайловый MMQ. ~20-40 мкс/слой + сжатие skew для B4.

**Честный потолок трека B:** B1-B5 возвращают ~250-330 из 345 мкс/слой →
EP decode **~паритет с layer split (в пределах 5-10%)**, но вряд ли лучше:
остаются ~30 мкс невыжимаемого reduce + дисбаланс top-6-of-64. Зато EP
тогда перестаёт быть «режимом для оффлайна»: prefill +21% И нормальный
decode в одном конфиге, а MTP поверх амортизирует остаток комма на шаг
(комм платится за шаг, не за токен).

**Что EP-decode НЕ вылечит (проверено расчётом):** verify-батчи цепочечного
MTP K≤3 сами по себе. Кроссовер требует c*≈1.8-4.0 → батчи 8-16 токенов =
деревья, а деревья раздувают union экспертов (×2-3 к чтениям весов) и в
llama.cpp-архитектуре непрактичны (EAGLE3 на MoE GPT-OSS: ×1.08). Сначала
резать комм (B1-B4), потом K=2 доводит до цели.

### Открытый вопрос по нашей MTP-реализации (проверить в коде)

Из памяти сессии 04.07 неоднозначно: verify-пара идёт одним nt=2 проходом
или «размоткой на два nt=1 const-shape прохода». Если второе — мы сейчас
НЕ получаем verify-batch амортизацию (ни весов, ни индексер-скана, ни
комма) и K=1 выигрывает только на бесплатных драфтах из статик-выходов.
Тогда починка nt=2-батчинга — отдельный жирный рычаг и для layer, и
особенно для EP. Смотреть `src/models/deepseek4.cpp` + spec-клиент.

---

## Часть 3. Целевая арифметика

| Конфиг | Сейчас | После гипотез |
|---|---:|---|
| Layer, мелкий ctx, no-MTP | 36.3 | — (база) |
| Layer + MTP K=1, мелкий | 37.5–44.8 (замерено) | A4+A5: ~40–47 |
| Layer, 97K, no-MTP | 31.6 | A3: ~36–43 |
| **Layer + MTP K=1, 97K** | **не мерено — A1!** | **оценка 39–47; +A3: 45–55** |
| + MTP K=2 adaptive | — | ×1.08–1.3 поверх K=1 |
| EP, мелкий, no-MTP | 23.8 | B1-B5: ~33–36 (паритет) |
| EP + MTP после B1-B5 | — | ~40+ при prefill 702 t/s |

Цель «35 на всей длине»: вероятно закрывается уже A1 (замер), гарантированно
— A1+A2 или A1+A3. Трек B — про то, чтобы не выбирать между prefill и
decode вообще.

## Часть 4. Порядок экспериментов (когда дойдём до рук)

1. **A1**: замер прод-конфига (MTP+fused idx) на 8K/32K/97K — 0 кода,
   один вечер. Заодно A5 (lock клоков) тем же стендом.
2. **B2** (тривиальный дифф) → перезамер EP decode на фикстуре.
3. **B1** (events) → фикстура → полная модель. После B1+B2 решение:
   стоит ли B3/B4.
4. **A2**: офлайн-тест приёмки D2 (аналог фазы A). Если conditional
   accept ≥0.4 — чинить nt=3 replay и делать adaptive K=2.
5. **B4**: кастомный reduce-to-home кернел (самый вкусный кусок written
   for X: «выкинули NCCL из decode»).
6. **A3**: прототип cross-layer index reuse под качественным гейтом
   (needle@200K + KL).
7. **A4**: порт upstream PR-ов по мере ребейза.

## Риски

- **K=2**: приёмка может реально оказаться низкой на агентных/long-ctx
  промптах (LMSYS мерил 0.35-0.4 на 65K) → adaptive-гейт обязателен.
- **A3**: качество отбора top-k — единственная гипотеза, трогающая
  качество генерации. Только под гейтом.
- **B1**: ломкость sched (n_copies, указатели) — держать за флагом,
  A/B на фикстуре до полной модели.
- **P2P-драйвер**: может слететь при апдейте ядра (DKMS) — B4 должен
  иметь fallback на NCCL-путь.
- GPU0 периодически занят vLLM (20 GB) — проверять nvidia-smi перед
  полномодельными замерами.

## Ссылки (ключевые)

- vLLM DSv4-Flash MTP: github.com/vllm-project/vllm/issues/43457
- NCCL 16KB AR на 4×PCIe P2P ~10.7 мкс: github.com/vllm-project/vllm/issues/36481
- IndexCache (cross-layer index reuse, ×1.48): arxiv.org/abs/2603.12201
- LSA (temporal re-index τ=64): arxiv.org/html/2606.09079
- Cohere MoE×SpecDec (verify 4 ток = ×1.25): cohere.com/blog/mixture-of-experts-models-get-more-from-speculative-decoding
- ik_llama.cpp graph parallel (4×3090 прецедент): github.com/ikawrakow/ik_llama.cpp/pull/1080
- JAX/XLA fused one-shot AR (+27% e2e decode): developer.nvidia.com/blog/optimizing-for-low-latency-communication-in-inference-workloads-with-jax-and-xla/
- llama.cpp EAGLE3 (MoE ×1.08 — деревья не наш путь): github.com/ggml-org/llama.cpp/pull/18039
- Power vs decode 6×3090: llmgarage.ai/power-limit-tokens-per-second-benchmark/
- nsys-профили на риге: /tmp/ep_prof.nsys-rep, /tmp/layer_prof.nsys-rep
